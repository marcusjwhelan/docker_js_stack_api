apiVersion: v1
kind: persistentVolumeClaim
metadata:
  name: mysql-pv-claim
spec:
  storageClassName: manual # refer to k8s provider ~change this~
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi